@model App_Mascota.ViewModel.MascotaViewModel

@{
    ViewData["Title"] = "Registrar tu Mascota";
}

<div class="container">
    <h1 class="text-center mb-4">Formulario de Registro de Mascotas</h1>
    <div class="row justify-content-center">
        <div class="col-md-6">
            <form asp-controller="Mascota" asp-action="Enviar" method="post">
                <div class="form-group">
                    <label asp-for="FormMascota.Nombre" class="control-label"></label>
                    <input asp-for="FormMascota.Nombre" class="form-control" />
                    <span asp-validation-for="FormMascota.Nombre" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="FormMascota.Raza" class="control-label"></label>
                    <input asp-for="FormMascota.Raza" class="form-control" />
                    <span asp-validation-for="FormMascota.Raza" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="FormMascota.Color" class="control-label"></label>
                    <input asp-for="FormMascota.Color" class="form-control" />
                    <span asp-validation-for="FormMascota.Color" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="FormMascota.FechaNacimiento" class="control-label"></label>
                    <input asp-for="FormMascota.FechaNacimiento" class="form-control" type="date" min="1900-01-01" max="2024-12-31" />
                    <span asp-validation-for="FormMascota.FechaNacimiento" class="text-danger"></span>
                </div>
                <div class="form-group text-center">
                    <button type="submit" class="btn btn-primary">Registrar Mascota</button>
                </div>
            </form>
        </div>
        <div class="col-md-6">
            <table class="table">
                <thead>
                    <tr>
                        <th>Nombre</th>
                        <th>Raza</th>
                        <th>Color</th>
                        <th>Fecha de Nacimiento</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.ListMascota != null && Model.ListMascota.Any())
                    {
                        foreach (var mascota in Model.ListMascota)
                        {
                            <tr>
                                <td>@mascota.Nombre</td>
                                <td>@mascota.Raza</td>
                                <td>@mascota.Color</td>
                                <td>@mascota.FechaNacimiento.ToShortDateString()</td>
                                <td>
                                    <a asp-action="Edit" asp-route-id="@mascota.Id" class="btn btn-warning btn-sm">Editar</a>
                                    <a asp-action="Delete" asp-route-id="@mascota.Id" class="btn btn-danger btn-sm">Eliminar</a>
                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
